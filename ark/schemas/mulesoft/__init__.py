"""MuleSoft connector schemas. See `http_connector.json`'s own top-level
`"notes"`/`"source"` fields for provenance — every field it asserts is
cited to a real docs.mulesoft.com page, not invented."""

from __future__ import annotations
