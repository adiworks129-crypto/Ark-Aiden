"""Ark's estate generator — Milestone 3.

Turns a GeneratorConfig into a deterministic, schema-valid GroundTruthEstate
representing a realistic (but deliberately "clean" — see Milestone 4 for
mutation) layered enterprise integration estate. See generator.py for the
top-level entry point, generate_estate().
"""
